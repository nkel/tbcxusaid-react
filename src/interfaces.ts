export default interface IRecipes {
    id: number,
    name: string,
    ingredients: string[]
    image: string,
    instructions: string
}
export default interface IArticle {
    id: number,
    title: string,
    body: string
}